// if(carscount = slots.length){
		// 	resize();
		// }
		// for (int i = 0;i < slots.length;i++) {
		// 	if(slots[i] == null){
		// 		slots[i] = new Slot(name);
		// 		break;
		// 	}
			
		// }


if (slots[i]!= null) {
			for (int i = 0;i < carscount;i++) {
				str += slots[i];
				
			}
			
		}

	}